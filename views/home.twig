
{{ include ('header.twig') }}

	<p> Bienvenu(e) {{name}} !</p>

	<ul>
		{% for fruit in fruits %}
			<li> {{ fruit.name}} est de couleur {{ fruit.color }} </li>

		{% endfor %}

	</ul>

	<a href="{{ path('helloworld') }}">Hello World</a>
	<a href=" {{ path('customers') }} ">Customers</a>
	<a href="">Dire Bonjour</a>
	<a href="{{ path('flickr') }}">Flickr</a>

</body>
